{
    "name": "Python & PostgreSQL",
    "dockerFile": "Dockerfile",
    "forwardPorts": [8000, 5432],
    "settings": {
        "terminal.integrated.shell.linux": "/bin/bash"
    },
    "postCreateCommand": "service postgresql start && sudo -u postgres psql -c \"CREATE DATABASE DATA;\" && sudo -u postgres psql -c \"CREATE USER admin WITH PASSWORD 'admin';\" && sudo -u postgres psql -c \"GRANT ALL PRIVILEGES ON DATABASE DATA TO admin;\"",
    "extensions": ["ms-python.python"]
}
